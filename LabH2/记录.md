``` C
bool flag = true;
while (flag) {
    flag = false;
    for (int i = 1; i < n; i++) {
        if (a[i] > a[i + 1]) {
            flag = true;
            int t = a[i];
            a[i] = a[i + 1];
            a[i + 1] = t;
        }
    }
}
```

转写为数字系统描述

``` pseudocode
beg:
	flag = 0
loop:
	i = 1
	p = a[i], q = a[i + 1]
	if (m > n) begin
		flag = 1
		a[i] = q
		a[i + 1] = p
	end if
	if (i < n - 1) begin
		i++
		goto loop
	end if
	if (flag) begin
		goto beg
	end if
	end
```

设计状态转换图：